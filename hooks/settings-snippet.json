{
  "_comment": "Merge this guard into ~/.claude/settings.json. install.sh does this automatically with the real repo path if jq is installed. Replace /path/to/agent-config if adding manually.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "/path/to/agent-config/hooks/deny-dangerous.sh"
          }
        ]
      }
    ]
  }
}
