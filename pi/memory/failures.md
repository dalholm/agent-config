[failure] Next.js 16 + Tailwind (any version: v3 or v4) fails to generate CSS — no .next/static/css output regardless of config. Tried v3, v4, switching between them, both installed simultaneously — all failed. Root cause: Next.js 16's bundler doesn't emit Tailwind CSS output. WORKAROUND: Abandon Tailwind entirely. Use plain CSS with CSS custom properties and hardcoded classes in JSX. Import globals.css explicitly in layout.tsx via `import "@/app/globals.css"`. <!-- created=2026-06-20, last=2026-06-20 -->
§
[failure] Sidebar component (Sidebar.tsx) uses hardcoded Tailwind utility classes (e.g. bg-[#18181b], text-slate-400, etc.) but globals.css was never imported in layout.tsx, so no styling appeared at all. Fix: add `import "@/app/globals.css"` to layout.tsx AND either (a) define matching CSS rules in globals.css, or (b) rewrite Sidebar to use CSS custom properties instead of Tailwind classes. <!-- created=2026-06-20, last=2026-06-20 -->
§
[failure] Tailwind config loop: spent 10+ iterations toggling between Tailwind v3/v4, rewriting configs, trying to force CSS into .next/static/css/ — when the project actually worked. User called it out: "varför ska du skriva om? det fungerar ju nu?" Lesson: don't rewrite working code based on assumptions about build output directories. <!-- created=2026-06-20, last=2026-06-20 -->