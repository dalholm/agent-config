# Claude Code entry point

My operating rules live in **AGENTS.md** — the single source of truth. Read it first
and follow it; it is the highest-priority context.

@./AGENTS.md

<!--
Note: the recommended install (install.sh) symlinks ~/.claude/CLAUDE.md straight to
AGENTS.md, so this pointer file is only used if you copy files in manually instead.
-->
